Allow forwarding to another port on localhost implicitly (by omitting the destination address)
Add rate-limiting for ICMP packets to help protect against ping floods
Process and validate ports instead of passing them directly to --dport
Test to see if NAT between LAN interfaces is necessary
Check /proc/sys/net/ipv4/ip_forward == 1 if using forwarding
Allow multiple --services and --forwards files
